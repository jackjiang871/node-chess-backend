Backend for chess app

To start the server type these commands into the terminal
1. `npm install`
2. `npm run build`
3. `npm run start` or `npm run dev` if you want to restart the server everytime you change the src file, uses nodemon